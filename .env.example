# Copie este arquivo para .env.local (não é versionado) e preencha com os
# valores do seu projeto Supabase: Project Settings > API.
# Sem essas variáveis, o app funciona normalmente, só 100% local (sem nuvem).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
